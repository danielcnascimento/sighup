import { IDefaultFormData } from "../../sign-up.stepper.section.types";
import { SetForm } from "react-hooks-helper";

export interface IBoardingSectionProps {
  formData: IDefaultFormData;
  setForm: SetForm;
}
